# Docker 运行时依赖 = base（只装运行期真正会 import 的东西）。
#
# 与 `requirements.txt` 的关系：那份是 `-r requirements-base.txt`（= base + 开发依赖），
# 这份只 `-r requirements-base.txt`，因此不装 pytest / pypdf / streamlit / pandas。
#
# ⚠️ 改 Dockerfile 时注意：`-r` 引用的文件必须一起 COPY 进镜像，
# 否则 `pip install -r requirements-runtime.txt` 会因找不到 base 而失败。
#
# 不使用本地 torch / transformers：嵌入与推理均走在线 API，镜像更小。

-r requirements-base.txt
