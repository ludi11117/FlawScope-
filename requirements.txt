# FlawScope 全量依赖（base + 开发/测试）。
#
# 保留 `requirements.txt` 这个文件名，是为了兼容既有脚本、文档与习惯用法
# （`pip install -r requirements.txt`）。内容只有一行 `-r`：
# 真正的版本清单在 `requirements-base.txt`，dev 追加在 `requirements-dev.txt`。
#
# 为什么不能再手抄：两份手抄清单的漂移是不可见的——只在一处升版本就会出现
# "本地测过的"和"镜像里跑的"不是同一个版本。现在只有 base 需要维护版本。

-r requirements-dev.txt
